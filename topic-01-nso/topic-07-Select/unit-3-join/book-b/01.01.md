# Joining multiple tables

Sometimes we need to retrieve information from more than two tables. The rule goes that the number of *JOINs* a Select statement has is one less than the number of tables involved. So if we have three tables in the query then we have two JOINs, if we have four tables in the query then we would have three JOINs and so on.